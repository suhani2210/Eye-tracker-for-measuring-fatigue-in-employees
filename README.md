# Setup and Run instructions

### Installation

```bash
# Clone or unzip the project, then navigate into it
cd eye-tracker-dashboard

# Install dependencies
npm install
```

### Run Commands

```bash
# Start development server (hot reload)
npm run dev
